# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. No code was changed and no builds or tests were run — all evidence was gathered
by reading source files, manifests, and git history.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`README.md` and `CLAUDE.md` both accurately describe the app as a GPU-accelerated particle field
canvas for iPhone and iPad using Metal compute, SwiftUI + UIKit, with 4 field node types and 4
particle behaviors. Entry point confirmed at `Chromafield/App/ChromafieldApp.swift`. Core modules
verified: Metal engine, simulation, input, UI overlay, export, persistence.

---

### 2. Current state

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`CLAUDE.md` states "v1.0 Feature Complete — All 3 phases implemented (Metal Engine, Interactive
Canvas, Export + Polish)." Verified by reading source:

- Phase 0 (Metal Engine): `MetalEngine.swift`, `ParticleBuffer.swift`, `SharedTypes.h`,
  `DeviceCapabilities.swift`, `FrameBudgetMonitor.swift` — all present and fully implemented.
- Phase 1 (Interactive Canvas): `CanvasContainerView.swift`, `GestureCoordinator.swift`,
  `PencilInputHandler.swift`, `InputMapper.swift`, `RadialNodeMenu.swift`, `BehaviorPicker.swift`,
  `PaletteSelector.swift` — all present. Trail accumulation confirmed in `MetalEngine.swift`.
- Phase 2 (Persistence, Presets, Behaviors): `ParticleBehavior.swift` (4 modes), `FieldConfig.swift`,
  `PersistenceManager.swift`, `PresetGallery.swift`, 6 bundled JSON preset files — all present.
- Phase 3 (Export + Polish): `OffscreenRenderer.swift`, `ImageExporter.swift`, `VideoExporter.swift`,
  `ExportControls.swift`, `CanvasHUD.swift`, adaptive quality in `MetalEngine.swift` — all present.

No stubs, incomplete feature flags, or TODOs blocking ship were found.

---

### 3. Stack

**Status:** `drifted` (fixed)
**Evidence:** `verified-by-reading-code`

`CLAUDE.md` lines 11 and 70 described the test suite as "(headless simulation correctness tests)",
but the actual test suite has 13 test files covering many more modules:
`AdaptiveQualityTests`, `BehaviorSimulationTests`, `ColorPaletteTests`,
`DeviceCapabilitiesTests`, `ExportTests`, `FieldManagerTests`, `FieldNodeModelTests`,
`FrameBudgetMonitorTests`, `InputMapperTests`, `OffscreenRendererTests`, `PersistenceTests`,
`PresetDecodeTests`, `SimulationTests`. (`ChromafieldTests/` verified by directory listing.)

**What changed:**
- `CLAUDE.md:11` — `(headless simulation correctness tests)` → `(simulation, persistence, input, export, and utility unit tests)`
- `CLAUDE.md:70` — same fix in the Portfolio Context section

All other stack entries (Swift 6.0 strict concurrency, SwiftUI iOS 17+, Metal/MetalKit, AVFoundation,
UIKit, no SPM dependencies, XcodeGen `project.yml`) verified consistent with `project.yml` and source.

---

### 4. How to run

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`README.md` Quick Start prescribes `xcodegen generate && open Chromafield.xcodeproj`. Verified:
`project.yml` exists at repo root with a valid XcodeGen configuration targeting iOS 17.0
(`project.yml:5`). The `Chromafield.xcodeproj` directory is also present.
The GitHub clone URL in the README is `unverifiable` from code alone (no remote URL was read),
but is consistent with git user `saagpatel` in the commit log.

---

### 5. Known risks

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

All risks in `CLAUDE.md` "Gotchas" and "Known Risks" sections match code reality:
- UIKit gesture ownership: `GestureCoordinator.swift` attaches `UIGestureRecognizer` to the
  underlying `UIView`, not through SwiftUI.
- Two-pass flocking: `MetalEngine.swift` encodes `clearGridPipeline` + `buildGridPipeline` passes
  before the main compute pass (`MetalEngine.swift:396–421`); no O(n²) neighbor scan.
- Offline offscreen export: `VideoExporter.swift` sets `isExporting = true` on `MetalEngine` and
  drives simulation via `engine.step()` calls — no live frame recording.
- No network entitlements: `project.yml` declares no network capability.
- v2 scope gate (GIF, audio, 4×, time-varying): none present in the codebase.

---

### 6. Next move

**Status:** `unverifiable`
**Evidence:** `unverifiable-because-forward-looking`

`CLAUDE.md` "Next Recommended Move" says: "Use this context plus the README and supporting docs to
resume the next active task, then promote the repo beyond minimum-viable by capturing a dedicated
handoff, roadmap, or discovery artifact." This is forward-looking operational guidance. The code
confirms v1.0 is feature complete with no open stubs, but cannot confirm what the operator intends
as the next step. Text left unchanged.

---

## Contradictions for Manual Review

The following are drifts in `IMPLEMENTATION-ROADMAP.md`, which is outside the editable scope of
this pass. A human should apply these corrections.

### 1. `IMPLEMENTATION-ROADMAP.md:96–103` — Particle struct sample includes `float4 color` that does not exist

The "Core Data Structures" section shows a `Particle` struct with `float4 color; // RGBA, updated
per frame via palette lookup` (16 bytes), then asserts `MemoryLayout<Particle>.size must == 32`.
With `color` the struct would be 48 bytes, making the assertion false.

The actual implementation at `Chromafield/Metal/Shaders/SharedTypes.h:6–13` has no `color` field;
the struct is correctly 32 bytes. Color is computed per-frame in the fragment shader using speed
and palette lookups, not stored per-particle.

**Fix:** Remove the `float4 color;` line (and its comment) from the Particle struct code sample.

---

### 2. `IMPLEMENTATION-ROADMAP.md:54` — `FieldNode.swift` should be `FieldNodeModel.swift`

The file structure diagram lists `Simulation/FieldNode.swift` but the actual file on disk is
`Chromafield/Simulation/FieldNodeModel.swift`.

**Fix:** Change `FieldNode.swift` → `FieldNodeModel.swift`.

---

### 3. `IMPLEMENTATION-ROADMAP.md:48` — `SharedTypes.metal` should be `SharedTypes.h`

The file structure diagram lists `Metal/Shaders/SharedTypes.metal` but the actual file is
`Chromafield/Metal/Shaders/SharedTypes.h` (a C header included via bridging header, not a Metal
source file). The `.metal` files are `ParticleCompute.metal` and `ParticleRender.metal`.

**Fix:** Change `SharedTypes.metal` → `SharedTypes.h`.

---

### 4. `IMPLEMENTATION-ROADMAP.md:604` — `DeviceCapabilityTests.swift` should be `DeviceCapabilitiesTests.swift`

The Testing Strategy table lists `DeviceCapabilityTests.swift` but the actual file is
`ChromafieldTests/DeviceCapabilitiesTests.swift` (plural "Capabilities").

**Fix:** Change `DeviceCapabilityTests.swift` → `DeviceCapabilitiesTests.swift`.

---

### 5. `IMPLEMENTATION-ROADMAP.md:600–605` — Testing Strategy table lists only 5 test files; 13 exist

The table mentions only: `SimulationTests`, `DeviceCapabilityTests`, `FrameBudgetMonitorTests`,
`PersistenceTests`, `InputMapperTests`. The actual suite also includes:
`AdaptiveQualityTests`, `BehaviorSimulationTests`, `ColorPaletteTests`, `ExportTests`,
`FieldManagerTests`, `FieldNodeModelTests`, `OffscreenRendererTests`, `PresetDecodeTests`.

**Fix:** Expand the table to reflect all 13 test files (or add a note that the table is illustrative).

---

### 6. `IMPLEMENTATION-ROADMAP.md:167–176` — `FieldConfig` struct sample omits `noiseScale` field

The code sample for `FieldConfig` shows `paletteIndex: Int` as the last field before
`thumbnailData`. The actual implementation at `Chromafield/Persistence/FieldConfig.swift:9`
includes `var noiseScale: Float` as a persisted field with a backward-compatible decoder default
of `0.5`. It is also present as a `CodingKey` (`FieldConfig.swift:34`) and in the `ContentView`
`loadConfig` and `saveCurrentConfig` paths.

**Fix:** Add `var noiseScale: Float` to the FieldConfig struct code sample.

---

## Footer

Generated: 2026-06-02 19:43:35 PDT
Branch: docs/truth-up-2026-06-02
HEAD sha reconciled against: f0f527fd80a25c5a0e59dfb19973903437fe874a
